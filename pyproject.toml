[tool.ruff]
target-version = "py312"
line-length = 100
src = ["services", "libs", "infra", "gateway", "frontend"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
